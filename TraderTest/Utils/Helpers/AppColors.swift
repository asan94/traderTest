//
//  AppColors.swift
//  TraderTest
//
//  Created by Asan Ametov on 01.03.2026.
//

import UIKit

enum AppColors {
    static let up = UIColor(hex: "85BA57") ?? UIColor.red.withAlphaComponent(0.9)
    static let down =  UIColor(hex: "A34E60") ?? UIColor.green.withAlphaComponent(0.9)
    static let neutral = UIColor.label

    static let highlightUp = UIColor(hex: "7DB556") ?? UIColor.green.withAlphaComponent(0.8)
    static let highlightDown = UIColor(hex: "D4475C") ?? UIColor.red.withAlphaComponent(0.8)
}
